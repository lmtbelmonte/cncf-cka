apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: minimal-ingress
spec:
  rules:
  - host: ghost.192.168.99.100.nip.io
    http:
      paths:
      - path: /testpath
        pathType: Prefix
        backend:
          service:
            name: ghost
            port:
              number: 2380
